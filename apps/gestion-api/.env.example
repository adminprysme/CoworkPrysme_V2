# MongoDB cluster
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_COWORK=cowork_bdd
MONGODB_DB_PRYSMA=prysma_bdd

# CORS — comma-separated browser origins (gestion-web + vitrine-api server-side callers if needed)
ALLOWED_ORIGIN=http://localhost:3002,http://localhost:8002

# Local dev port (default: 8003)
PORT=8003
